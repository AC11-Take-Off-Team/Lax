<section class="w-full h-full fixed left-0 top-0 mx-auto flex justify-center items-center bg-current">
  <div class="w-1/2 p-2.5 bg-white text-center">
    <h2>創建頻道</h2>
    <h2>創建私人頻道</h2>
    <p>频道是你的團隊進行通信的地方。圍繞主题進行組織時其效果最佳，例如#市場營銷#。</p>

    <%= form_for @group do |form| %>
      <div>
        名稱：<%= form.text_field :title %>
      </div>
      <div>
        描述：<%= form.text_field :description %>
      </div>
      <%= form.submit %>
    <% end %>

    <%# <div class="container relative top-2/4 left-1/2 h-12 flex items-center justify-center translate-x-2/4">
      <div class="toggle absolute top-0 left-0 w-10 h-7 bg-black rounded-3xl transition",onclick="Touch">
        <div class="button absolute top-1 left-1 w-4 h-4 rounded-2xl cursor-pointer transition bg-white"></div>
      </div>
      <div class="text absolute">設為私人,當频道設置為私人時，只有受邀請之後才能查看或加入。</div>
    </div>
  </div> %>
</section>

<%# <script>

  let toggle = document.querySelector(".toggle")
  let text = document,querySelector(".text")

  function Touch(){
    toggle.classList.toggle("active")
    if(toggle.classList.contains("active")){
      text.innerHTML = "設為私人
    這無法撤銷。私人频道之后不能變為公共频道。"
    }
    else
      text.innerHTML = "設為私人,當频道設置為私人時，只有受邀請之後才能查看或加入。"
  }
</script> %>






