<div  class=" h-96 ">
  <div class="h-4/5 ">
    <div class="border-b-2 ">
      <h3 class="text-5xl ml-10 mb-3"><%= @group.title %><h3>
    </div>
  </div>
  <% if current_user.join?(@group) %>
    <div class="my-6 text-2xl text-blue-400 font-semibold ml-10 mt-8"><%= @group.description %><%= link_to "編輯說明" , edit_group_path(@group) %></div>
  <% else %>
    <div class="bg-gray-200 h-full flex flex-col justify-center items-center" >
      <div class="text-2xl p-3 my-2"><%= @group.title %></div>
      <p class="mb-5 text-xl text-gray-600">這個頻道適用於<%= @group.title %>的所有內容,與你的團隊一起召開會議.共享文檔做出決策</p>
      <div>
        <button class="bg-gray-300 hover:bg-gray-400 py-3 px-8 text-black border-b-4 rounded-lg border-gray-500 transition duration-300 hover:border-gray-700"><%= link_to "詳情" , content_group_path(@group) %></button>
        <button class="bg-yellow-600 hover:bg-yellow-700 py-3 px-8 text-yellow-100 border-b-4 rounded-lg border-yellow-700 transition duration-300 hover:border-yellow-800"><%= link_to "加入頻道" , join_group_path(@group),method: "post" %></button>
      </div>
      <div class="text-gray-400 mt-4"><%= link_to "返回所有頻道" , groups_path %></div>
    </div>
  <% end %>
</div>

