<div  class=" h-96">
    <div class="">
      <h2 class="text-gray-500 text-lg mb-1 font-extrabold p-6"><p><%= "Current group: #{@room.name}"%></p></h2>

  </div>
  <% if current_user.join?(@group) %>
       <div class="px-6 py-4 flex-1 overflow-y-scroll" style="height: 350px">
       <div id = "messages">
    <% @room.messages.each do |message| %>
        <%= render 'messages/message', message: message %>          
    <%end %>
    </div>
    </div>
    <%= render 'messages/form', message: Message.new, room: @room %>
  <% else %>
    <div class="flex flex-col items-center justify-center h-full bg-gray-200" >
      <div class="p-3 my-2 text-2xl"><%= @group.title %></div>
      <p class="mb-5 text-xl text-gray-600">這個頻道適用於<%= @group.title %>的所有內容,與你的團隊一起召開會議.共享文檔做出決策</p>
      <div>
        <button class="px-8 py-3 text-black transition duration-300 bg-gray-300 border-b-4 border-gray-500 rounded-lg hover:bg-gray-400 hover:border-gray-700"><%= link_to "詳情" , content_group_path(@group) %></button>
        <button class="px-8 py-3 text-yellow-100 transition duration-300 bg-yellow-600 border-b-4 border-yellow-700 rounded-lg hover:bg-yellow-700 hover:border-yellow-800"><%= link_to "加入頻道" , join_group_path(@group),method: "post" %></button>
      </div>
      <div class="mt-4 text-gray-400"><%= link_to "返回所有頻道" , groups_path %></div>
    </div>
  <% end %>
</div>

